const express = require("express");

// express app 
const app = express();








// port
const port = 3000;
// listening to port
app.listen(port, console.log(`server listening to port ${port}...`));